[gd_scene load_steps=14 format=2]

[ext_resource path="res://assets/sea_64px.png" type="Texture" id=1]
[ext_resource path="res://sea.gd" type="Script" id=2]
[ext_resource path="res://waves.shader" type="Shader" id=3]
[ext_resource path="res://sea.shader" type="Shader" id=4]
[ext_resource path="res://assets/wave.png" type="Texture" id=6]
[ext_resource path="res://assets/noise.png" type="Texture" id=7]

[sub_resource type="Shader" id=8]
code = "/*
Shader from Godot Shaders - the free shader library.
godotshaders.com/shader/god-rays

Feel free to use, improve and change this shader according to your needs
and consider sharing the modified result on godotshaders.com.
*/

shader_type canvas_item;

uniform float angle = -0.3;
uniform float position = -0.2;
uniform float spread : hint_range(0.0, 1.0) = 0.5;
uniform float cutoff : hint_range(-1.0, 1.0) = 0.1;
uniform float falloff : hint_range(0.0, 1.0) = 0.2;
uniform float edge_fade : hint_range(0.0, 1.0) = 0.15;

uniform float speed = 1.0;
uniform float ray1_density = 8.0;
uniform float ray2_density = 30.0;
uniform float ray2_intensity : hint_range(0.0, 1.0) = 0.3;

uniform vec4 color : hint_color = vec4(1.0, 0.9, 0.65, 0.8);

uniform bool hdr = false;
uniform float seed = 5.0;

// Random and noise functions from Book of Shader's chapter on Noise.
float random(vec2 _uv) {
    return fract(sin(dot(_uv.xy,
                         vec2(12.9898, 78.233))) *
        43758.5453123);
}

float noise (in vec2 uv) {
    vec2 i = floor(uv);
    vec2 f = fract(uv);

    // Four corners in 2D of a tile
    float a = random(i);
    float b = random(i + vec2(1.0, 0.0));
    float c = random(i + vec2(0.0, 1.0));
    float d = random(i + vec2(1.0, 1.0));


    // Smooth Interpolation

    // Cubic Hermine Curve. Same as SmoothStep()
    vec2 u = f * f * (3.0-2.0 * f);

    // Mix 4 corners percentages
    return mix(a, b, u.x) +
            (c - a)* u.y * (1.0 - u.x) +
            (d - b) * u.x * u.y;
}

mat2 rotate(float _angle){
    return mat2(vec2(cos(_angle), -sin(_angle)),
                vec2(sin(_angle), cos(_angle)));
}

vec4 screen(vec4 base, vec4 blend){
	return 1.0 - (1.0 - base) * (1.0 - blend);
}

void fragment()
{
	
	// Rotate, skew and move the UVs
	vec2 transformed_uv = ( rotate(angle) * (UV - position) )  / ( (UV.y + spread) - (UV.y * spread) );
	
	// Animate the ray according the the new transformed UVs
	vec2 ray1 = vec2(transformed_uv.x * ray1_density + sin(TIME * 0.1 * speed) * (ray1_density * 0.2) + seed, 1.0);
	vec2 ray2 = vec2(transformed_uv.x * ray2_density + sin(TIME * 0.2 * speed) * (ray1_density * 0.2) + seed, 1.0);
	
	// Cut off the ray's edges
	float cut = step(cutoff, transformed_uv.x) * step(cutoff, 1.0 - transformed_uv.x);
	ray1 *= cut;
	ray2 *= cut;
	
	// Apply the noise pattern (i.e. create the rays)
	float rays;
	
	if (hdr){
		// This is not really HDR, but check this to not clamp the two merged rays making 
		// their values go over 1.0. Can make for some nice effect
		rays = noise(ray1) + (noise(ray2) * ray2_intensity);
	}
	else{
		 rays = clamp(0., 1., noise(ray1) + (noise(ray2) * ray2_intensity));
	}
	
	// Fade out edges
	rays *= smoothstep(0.0, falloff, (1.0 - UV.y)); // Bottom
	rays *= smoothstep(0.0 + cutoff, edge_fade + cutoff, transformed_uv.x); // Left
	rays *= smoothstep(0.0 + cutoff, edge_fade + cutoff, 1.0 - transformed_uv.x); // Right
	
	// Color to the rays
	vec3 shine = vec3(rays) * color.rgb;

	// Try different blending modes for a nicer effect. \"Screen\" is included in the code,
	// but take a look at https://godotshaders.com/snippet/blending-modes/ for more.
	// With \"Screen\" blend mode:
	shine = screen(texture(SCREEN_TEXTURE, SCREEN_UV), vec4(color)).rgb;
	
	COLOR = vec4(shine, rays * color.a);
}"

[sub_resource type="ShaderMaterial" id=9]
shader = SubResource( 8 )
shader_param/angle = 0.252
shader_param/position = 0.0
shader_param/spread = 1.0
shader_param/cutoff = 0.1
shader_param/falloff = 0.428
shader_param/edge_fade = 0.71
shader_param/speed = 4.0
shader_param/ray1_density = 8.0
shader_param/ray2_density = 30.0
shader_param/ray2_intensity = 1.0
shader_param/color = Color( 1, 1, 1, 0.8 )
shader_param/hdr = true
shader_param/seed = 5.0

[sub_resource type="ShaderMaterial" id=1]
shader = ExtResource( 4 )
shader_param/tile_factor = Vector2( 37.5, 25 )
shader_param/aspect_ratio = 0.666667

[sub_resource type="CanvasItemMaterial" id=3]
blend_mode = 1

[sub_resource type="ShaderMaterial" id=6]
shader = ExtResource( 3 )
shader_param/tile_factor = Vector2( 150, 100 )
shader_param/aspect_ratio = 0.5

[sub_resource type="ShaderMaterial" id=5]

[sub_resource type="CanvasItemMaterial" id=7]
light_mode = 2

[node name="sea" type="Node2D"]
scale = Vector2( 2, 2 )

[node name="CanvasLayer" type="CanvasLayer" parent="."]
scale = Vector2( 2, 2 )
transform = Transform2D( 2, 0, 0, 2, 0, 0 )
follow_viewport_enable = true

[node name="rays" type="Sprite" parent="CanvasLayer"]
show_behind_parent = true
material = SubResource( 9 )
position = Vector2( 93.1871, 51.5464 )
scale = Vector2( 0.720256, 0.254394 )
z_index = 1
texture = ExtResource( 7 )

[node name="sea" type="Sprite" parent="."]
show_behind_parent = true
material = SubResource( 1 )
position = Vector2( 1200, 800 )
scale = Vector2( 37.5, 25 )
texture = ExtResource( 1 )
script = ExtResource( 2 )

[node name="lightnoise" type="Sprite" parent="."]
material = SubResource( 3 )
position = Vector2( 1238.89, 870.917 )
scale = Vector2( 4.91499, 3.36733 )
texture = ExtResource( 7 )
normal_map = ExtResource( 7 )

[node name="waves" type="Sprite" parent="lightnoise"]
modulate = Color( 1, 1, 1, 0.0823529 )
self_modulate = Color( 1, 1, 1, 0.839216 )
show_behind_parent = true
material = SubResource( 6 )
position = Vector2( -0.472672, -20.5231 )
scale = Vector2( 30.5189, 29.6971 )
texture = ExtResource( 6 )

[node name="ColorRect" type="ColorRect" parent="."]
modulate = Color( 1, 1, 1, 0.552941 )
show_behind_parent = true
material = SubResource( 5 )
use_parent_material = true
margin_left = 78.1537
margin_top = -4.19971
margin_right = 2569.15
margin_bottom = 1687.8
color = Color( 0, 0.564706, 0.741176, 1 )
__meta__ = {
"_edit_use_anchors_": false
}

[node name="CanvasModulate" type="CanvasModulate" parent="."]
modulate = Color( 1, 1, 1, 0.858824 )
material = SubResource( 7 )
